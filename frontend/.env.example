# =====================================================================
# VIGNAI OS FRONTEND — PRODUCTION ENVIRONMENT TEMPLATE
# Set this in your Vercel / Netlify / Cloudflare Pages project settings.
# =====================================================================

# Full production backend API base URL with /api path
# Example: https://vignai-backend.onrender.com/api
# Leave empty for relative '/api' if hosted behind the same domain/reverse proxy
VITE_API_BASE_URL=https://vignai-backend.onrender.com/api
